# Model Weights

Place the trained model files in this directory before starting the backend.

Required filenames:

- `best_model_1.keras`
- `best_model_2.keras`

The backend expects these exact names when loading the DenseNet121 and ResNet50 checkpoints.
